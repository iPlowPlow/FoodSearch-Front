import { CustomValidatorUtils } from './custom-validator-utils';

describe('MustMatch', () => {
  it('should create an instance', () => {
    expect(new CustomValidatorUtils()).toBeTruthy();
  });
});
